// Central place for tweakable gameplay / GPS thresholds.

export const GPS_MAX_SPEED_M_PER_S = 50; // ignore jumps faster than this
export const GPS_MIN_STEP_METERS = 2; // ignore tiny jitter below this distance

export const FOG_BRUSH_RADIUS_PX = 20;
export const FOG_OPACITY = 0.72;

